[pytest]
asyncio_mode = auto
testpaths = tests

# Fixtures build and dispose their own engine per test (see conftest), so a
# function-scoped loop is both correct and the future pytest-asyncio default.
asyncio_default_fixture_loop_scope = function
